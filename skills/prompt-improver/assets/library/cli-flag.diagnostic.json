{
  "id": "cli-flag.diagnostic",
  "schema": 1,
  "provenance": {
    "authored_by": "claude (investigation session) from blind-judge critiques; guards calibrated live; revised for robustness by opus via bench/jev/authoring/revise.sh",
    "reviewed_by": null,
    "created": "2026-09-23"
  },
  "match": "Add a command-line flag to an existing tool or script that makes it print extra diagnostic, trace, debug or progress information",
  "task_name": "add-{flag}-flag",
  "key_slots": [
    "flag"
  ],
  "fit": [
    {
      "id": "adds_flag",
      "q": "Does the request add a new command-line flag or option to an existing tool or script?",
      "min": 0.6
    },
    {
      "id": "reports_info",
      "q": "Is the purpose of the new flag to make the tool print extra information about what it is doing?",
      "min": 0.6
    }
  ],
  "requires": [
    {
      "id": "g_library",
      "q": "Is the file described in `target.usage` a library that other scripts load, rather than a command people run directly?",
      "max": 0.35
    }
  ],
  "escalate": [
    {
      "id": "g_internal",
      "q": "To add the new flag well, does an engineer first need to understand branching or fallback logic inside the tool?",
      "min": 0.6,
      "gap": "approach"
    }
  ],
  "slots": {
    "tool": {
      "kind": "entity",
      "accept": [
        "file",
        "path",
        "code",
        "identifier",
        "word"
      ],
      "q": "Which candidate names the existing tool or script that gets the new flag?"
    },
    "flag": {
      "kind": "entity",
      "accept": [
        "flag"
      ],
      "q": "Which candidate is the new flag, exactly as it should be typed?"
    },
    "desired": {
      "kind": "role",
      "role": "desired"
    },
    "reported": {
      "kind": "word",
      "q": "Which single noun names one of the things the new flag prints a line about (for example file, check, step, rule, attempt)?"
    }
  },
  "items": [
    {
      "id": "desc-base",
      "section": "description",
      "text": "Add a `{flag}` option to `{target_path}` that makes it print diagnostic lines on stderr about what it is doing, without changing its normal output."
    },
    {
      "id": "desc",
      "section": "description",
      "text": "Add `{flag}` to `{tool}` so that it {desired}."
    },
    {
      "id": "current",
      "section": "current",
      "text": "`{target_path}`: {target_summary}. It has no `{flag}` option today.",
      "guards": [
        {
          "id": "f_flag_absent",
          "fact": "target_lacks",
          "text": "{flag}"
        }
      ]
    },
    {
      "id": "current-has",
      "section": "current",
      "text": "`{target_path}`: {target_summary}. The text `{flag}` already appears in it; find out what it does today before adding anything.",
      "guards": [
        {
          "id": "f_flag_present",
          "fact": "target_has",
          "text": "{flag}"
        }
      ]
    },
    {
      "id": "desired-base",
      "section": "desired",
      "text": "`{target_name} {flag}` prints `[{target_stem}]` diagnostic lines on stderr; without `{flag}` the stdout and exit codes of `{target_name}` are unchanged."
    },
    {
      "id": "desired",
      "section": "desired",
      "text": "`{tool} {flag}` {desired}; without `{flag}` the output and exit codes are unchanged."
    },
    {
      "id": "appr-read",
      "section": "approach",
      "text": "Before editing, read `{target_path}` end to end: find where options are parsed, where the usage text lives, and every point the new diagnostics must cover, including points inside helpers, loops, subshells and command substitutions."
    },
    {
      "id": "appr-sites",
      "section": "approach",
      "text": "List every place `{flag}` must report on before writing any code, and keep that list to check coverage against at the end.",
      "guards": [
        {
          "id": "g_sites",
          "q": "Does the new flag report on fixed checks or steps written in the code, each of which runs or is skipped?",
          "min": 0.6
        }
      ]
    },
    {
      "id": "appr-attempts",
      "section": "approach",
      "text": "Before editing, trace every path through `{target_path}` that makes an attempt the flag must report, including retries, fallbacks and early exits, so that no attempt goes unreported.",
      "guards": [
        {
          "id": "g_attempts",
          "q": "Does the new flag report on a sequence of attempts, where each attempt can succeed or fail?",
          "min": 0.6
        }
      ]
    },
    {
      "id": "appr-design",
      "section": "approach",
      "text": "Choose one design and commit to it: a single logging helper called at each reporting point (preferred when the points are scattered), or wrapping an existing helper those points already share. Criteria: every point covered, smallest diff, default output untouched."
    },
    {
      "id": "req-desired",
      "section": "requirements.behaviour",
      "text": "`{tool} {flag}` {desired}."
    },
    {
      "id": "req-default",
      "section": "requirements.behaviour",
      "text": "Without `{flag}`, the standard output and exit codes of `{target_name}` are identical to today's."
    },
    {
      "id": "req-stderr",
      "section": "requirements.behaviour",
      "text": "Diagnostic output from `{flag}` goes to stderr only, so anything that consumes stdout is unaffected."
    },
    {
      "id": "fmt-prefix",
      "section": "requirements.format",
      "text": "Each diagnostic line has a stable, greppable prefix and shape: `[{target_stem}] <event> <subject> -> <result>`."
    },
    {
      "id": "fmt-hitmiss",
      "section": "requirements.format",
      "text": "Report what did not happen as well as what did: results are `hit`, `miss`, or `skipped (<reason>)`, so \"not checked\" is distinguishable from \"checked and absent\".",
      "guards": [
        {
          "id": "g_lookup",
          "q": "Is each thing the new flag reports on a lookup or check that either finds something or comes up empty?",
          "min": 0.6
        },
        {
          "id": "g_attempts",
          "q": "Does the new flag report on a sequence of attempts, where each attempt can succeed or fail?",
          "max": 0.55
        }
      ]
    },
    {
      "id": "fmt-attempts",
      "section": "requirements.format",
      "text": "Each attempt is reported in order with what was tried and how it ended (for example `ok`, `retry (<reason>)`, `limit (<reason>)`, `error (<exit code>)`), including attempts that were skipped.",
      "guards": [
        {
          "id": "g_attempts",
          "q": "Does the new flag report on a sequence of attempts, where each attempt can succeed or fail?",
          "min": 0.6
        }
      ]
    },
    {
      "id": "fmt-summary",
      "section": "requirements.format",
      "text": "Finish with one summary line on stderr giving the totals: how many {reported} items were reported and how each ended."
    },
    {
      "id": "if-positional",
      "section": "requirements.interface",
      "text": "`{flag}` is accepted before or after the positional arguments.",
      "guards": [
        {
          "id": "g_positional",
          "q": "Does the tool described in `target.usage` take positional (non-flag) arguments?",
          "min": 0.6
        }
      ]
    },
    {
      "id": "if-anywhere",
      "section": "requirements.interface",
      "text": "`{flag}` can appear anywhere among the existing options.",
      "guards": [
        {
          "id": "g_positional",
          "q": "Does the tool described in `target.usage` take positional (non-flag) arguments?",
          "max": 0.4
        }
      ]
    },
    {
      "id": "if-help",
      "section": "requirements.interface",
      "text": "`{flag}` is listed in the usage/help text of `{target_name}` in the existing format, with a one-line description."
    },
    {
      "id": "ex-normal",
      "section": "example",
      "input": "a normal run of `{target_name}` with `{flag}`",
      "output": "stderr has one `[{target_stem}]` line for each {reported}, then the summary line; stdout is unchanged",
      "reasoning": "Diagnostics are the feature; keeping them off stdout protects every consumer of the normal output."
    },
    {
      "id": "ex-skipped",
      "section": "example",
      "input": "a run of `{target_name}` with `{flag}` where one {reported} cannot run",
      "output": "that {reported} is reported as `skipped (<reason>)`",
      "reasoning": "Something that never ran must still be visible, or users cannot tell \"not checked\" from \"absent\".",
      "guards": [
        {
          "id": "g_sites",
          "q": "Does the new flag report on fixed checks or steps written in the code, each of which runs or is skipped?",
          "min": 0.6
        }
      ]
    },
    {
      "id": "ex-default",
      "section": "example",
      "input": "the same run of `{target_name}` without `{flag}`",
      "output": "no `[{target_stem}]` lines on stderr; stdout and the exit code identical to before the change",
      "reasoning": "The default path must not change at all."
    },
    {
      "id": "ex-recovered",
      "section": "example",
      "input": "a run with `{flag}` where the first attempt fails and a later one succeeds",
      "output": "stderr shows one line per attempt, in order, each with its outcome, then the summary; stdout carries the normal result",
      "reasoning": "The trace exists to explain what happened across attempts, including the failures that were recovered from.",
      "guards": [
        {
          "id": "g_attempts",
          "q": "Does the new flag report on a sequence of attempts, where each attempt can succeed or fail?",
          "min": 0.6
        }
      ]
    },
    {
      "id": "ver-syntax",
      "section": "verification",
      "text": "`{syntax_check}` succeeds."
    },
    {
      "id": "ver-grep",
      "section": "verification",
      "text": "`grep -n -F -- '{flag}' {target_path}` shows at least two matches: the option parsing and the usage/help text."
    },
    {
      "id": "ver-help",
      "section": "verification",
      "text": "The usage/help text printed by `{target_path}` lists `{flag}` with a one-line description."
    },
    {
      "id": "ver-diff",
      "section": "verification",
      "text": "`diff <({runner} {target_path} 2>/dev/null) <({runner} {target_path} {flag} 2>/dev/null)` produces no output.",
      "guards": [
        {
          "id": "g_required_args",
          "q": "Does the tool described in `target.usage` require at least one argument or option to be given?",
          "max": 0.35
        },
        {
          "id": "g_stdin",
          "q": "Does the tool described in `target.usage` read its input from standard input when no file argument is given?",
          "max": 0.35
        }
      ]
    },
    {
      "id": "ver-lines",
      "section": "verification",
      "text": "`{runner} {target_path} {flag} 2>&1 >/dev/null` prints one or more `[{target_stem}]` lines and the summary line.",
      "guards": [
        {
          "id": "g_required_args",
          "q": "Does the tool described in `target.usage` require at least one argument or option to be given?",
          "max": 0.35
        },
        {
          "id": "g_stdin",
          "q": "Does the tool described in `target.usage` read its input from standard input when no file argument is given?",
          "max": 0.35
        }
      ]
    },
    {
      "id": "ver-nolines",
      "section": "verification",
      "text": "`{runner} {target_path} 2>&1 >/dev/null` prints no `[{target_stem}]` lines.",
      "guards": [
        {
          "id": "g_required_args",
          "q": "Does the tool described in `target.usage` require at least one argument or option to be given?",
          "max": 0.35
        },
        {
          "id": "g_stdin",
          "q": "Does the tool described in `target.usage` read its input from standard input when no file argument is given?",
          "max": 0.35
        }
      ]
    },
    {
      "id": "ver-args",
      "section": "verification",
      "text": "Run `{target_path}` with the smallest valid arguments from its usage text, once with and once without `{flag}`: stdout and the exit code are identical, and only the flagged run prints `[{target_stem}]` lines on stderr.",
      "guards": [
        {
          "id": "g_required_args",
          "q": "Does the tool described in `target.usage` require at least one argument or option to be given?",
          "min": 0.65
        }
      ]
    },
    {
      "id": "ver-stdin",
      "section": "verification",
      "text": "Pipe the same small valid input into `{runner} {target_path}` once with and once without `{flag}`: stdout and the exit code are identical, and only the flagged run prints `[{target_stem}]` lines on stderr.",
      "guards": [
        {
          "id": "g_stdin",
          "q": "Does the tool described in `target.usage` read its input from standard input when no file argument is given?",
          "min": 0.65
        },
        {
          "id": "g_required_args",
          "q": "Does the tool described in `target.usage` require at least one argument or option to be given?",
          "max": 0.35
        }
      ]
    },
    {
      "id": "comp-test",
      "section": "companion",
      "file": "{test_file}",
      "text": "Add a regression check next to the existing coverage for `{target_name}`: stdout and exit code are identical with and without `{flag}`, and stderr contains `[{target_stem}]` only with the flag."
    },
    {
      "id": "comp-changelog",
      "section": "companion",
      "file": "{changelog}",
      "text": "Add an entry describing `{flag}`."
    },
    {
      "id": "con-callers",
      "section": "constraint",
      "text": "Callers of `{target_name}` ({callers}) keep calling it without `{flag}`; do not change them."
    },
    {
      "id": "con-order",
      "section": "constraint",
      "text": "Do not reorder or reword any existing output of `{target_name}`."
    },
    {
      "id": "con-existing",
      "section": "constraint",
      "text": "The flag only reports on {reported} items that already exist; do not add new ones.",
      "guards": [
        {
          "id": "g_sites",
          "q": "Does the new flag report on fixed checks or steps written in the code, each of which runs or is skipped?",
          "min": 0.6
        }
      ]
    },
    {
      "id": "con-secrets",
      "section": "constraint",
      "text": "Never print request bodies, credentials or other user data in diagnostic lines; report names, counts and outcomes only.",
      "guards": [
        {
          "id": "g_user_data",
          "q": "Could the information the new flag reports on include user-provided text, credentials or other sensitive data?",
          "min": 0.6
        }
      ]
    },
    {
      "id": "oos-timing",
      "section": "out_of_scope",
      "text": "Timing or profiling information for each {reported}."
    },
    {
      "id": "oos-timing-base",
      "section": "out_of_scope",
      "text": "Timing, profiling, or log levels beyond the single `{flag}` switch."
    },
    {
      "id": "esc-restructure",
      "section": "escape",
      "text": "If some {reported} items cannot be reported without restructuring the code (for example inside command substitutions or subshells), list them and ask before restructuring; do not skip them silently.",
      "guards": [
        {
          "id": "g_sites",
          "q": "Does the new flag report on fixed checks or steps written in the code, each of which runs or is skipped?",
          "min": 0.6
        }
      ]
    },
    {
      "id": "esc-paths",
      "section": "escape",
      "text": "If some attempt paths (for example early exits or fallbacks) cannot be reported without changing their behaviour, list them and ask instead of reshaping the control flow.",
      "guards": [
        {
          "id": "g_attempts",
          "q": "Does the new flag report on a sequence of attempts, where each attempt can succeed or fail?",
          "min": 0.6
        }
      ]
    },
    {
      "id": "esc-generic",
      "section": "escape",
      "text": "If the request is ambiguous, contradicts what the code shows, or would change the default output, stop and ask rather than guessing or working around it."
    },
    {
      "id": "chk-base",
      "section": "check",
      "text": "Re-read `{target_path}` and confirm `{flag}` is parsed and listed in the usage text, every diagnostic line is written to stderr, and no existing output line was reordered or reworded."
    },
    {
      "id": "chk-covered",
      "section": "check",
      "text": "Re-read `{target_path}` and confirm every {reported} item is reported, and that all diagnostics go to stderr."
    }
  ],
  "required": {
    "description": 1,
    "desired": 1,
    "approach": 2,
    "verification": 3,
    "check": 1,
    "escape": 1
  }
}
