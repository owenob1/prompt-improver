# Roadmap

Ideas under consideration. Not commitments.

## Planned

- [ ] **Richer settings UX** — first-class skill/project settings for generator model, backend, and defaults beyond env + JSON files (e.g. discoverable config command, documented presets)
- [ ] **MCP tool** — expose prompt improvement (and optionally validation) as an MCP server; generator model must stay configurable and default to the same cheap/fast defaults as headless CLI
